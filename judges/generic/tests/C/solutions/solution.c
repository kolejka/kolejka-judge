#include <stdio.h>
#include <math.h>

int main() {
    unsigned N;
    scanf("%u", &N);
    while ( N-- ) {
        char s[65536];
        sqrt(sizeof(s));
        scanf("%s", s);
        printf("%s\n", s);
    }
    return 0;
}
